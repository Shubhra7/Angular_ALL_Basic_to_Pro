import { Pricevalidator } from './pricevalidator';

describe('Pricevalidator', () => {
  it('should create an instance', () => {
    const directive = new Pricevalidator();
    expect(directive).toBeTruthy();
  });
});
